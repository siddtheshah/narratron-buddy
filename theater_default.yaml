agent:
    proactivity: false
    affective_dialog: false
    special_instructions: "be calm, serene, and encouraging to the orator"
    style: "wistful and bygone glories"

image_generation:
    cooldown_duration: 9
    # Gemini image models are published on Vertex's global endpoint. This is
    # intentionally separate from GOOGLE_CLOUD_LOCATION used by Gemini Live.
    location: global

animation:
    enabled: false
    cooldown_duration: 20

music:
    playlists_folder: "playlists"
    cooldown_duration: 15

named_elements: {}

chat:
    cooldown_duration: 9
