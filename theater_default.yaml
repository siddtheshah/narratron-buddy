agent:
    proactivity: false
    affective_dialog: false
    special_instructions: "be calm, serene, and encouraging to the orator"

observability_tool:
    enabled: false
    cooldown_duration: 30

image_generation:
    cooldown_duration: 9
    style: "wistful and bygone glories"

animation:
    enabled: false
    cooldown_duration: 20

music:
    generation_enabled: false
    playlists_folder: "playlists"
    generation_cooldown: 90
    switch_cooldown: 15
    style: "no lyrics, instrumental focus, steady volume, 90 second duration, loopable"

story_planning:
    nodes_ahead: 3
    adventure_mode: false
    max_named_elements: 5
    cooldown_duration: 10
    # Each completed planner response adds one second per 5 returned words,
    # up to this limit, before accepting the next player action.
    action_cooldown_words_per_second: 5
    action_cooldown_max_seconds: 30
    initial_elements: {}

chat:
    cooldown_duration: 9
